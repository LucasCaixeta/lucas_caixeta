{
  "name": "lucascaixeta-portfolio",
  "version": "2.0.0",
  "description": "Personal portfolio website of Lucas Caixeta - Mobile & Web Developer",
  "type": "module",
  "scripts": {
    "dev": "node --watch build/dev-server.js",
    "build": "node build/build.js",
    "test": "echo 'No tests configured yet' && exit 0",
    "test:watch": "echo 'No tests configured yet' && exit 0",
    "format": "prettier --write \"**/*.{js,html,css,json,md}\" --ignore-path .gitignore 2>/dev/null || echo 'Prettier not installed'",
    "lint": "eslint **/*.js --ignore-path .gitignore 2>/dev/null || echo 'ESLint not installed'"
  },
  "keywords": [
    "portfolio",
    "web developer",
    "mobile developer",
    "lucas caixeta",
    "frontend"
  ],
  "author": "Lucas Caixeta",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "prettier": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
